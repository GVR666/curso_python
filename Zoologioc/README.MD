PASOS PARA EJECUTAR EL PROGRAMA
1. Colocar `clases.csv`, `zoo.csv`, `funciones.py` y `main.py` en la carpeta `zoologico`.
2. Ejecutar `python main.py`.
3. Al salir (opción 4), se guardarán los cambios en el archivo CSV.